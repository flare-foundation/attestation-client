{
    // Ports for running verifier server.
    // NOTE: In docker deployment all those ports should remain 9500
    "BTCVerifierRouterServerPort": "9500",
    "XRPVerifierRouterServerPort": "9500",
    "DOGEVerifierRouterServerPort": "9500",

    // API Keys for verifier servers.
    // If more keys are to be added, add more unique stub entries here and more entries in 
    // `credentials.prepared/<chain>-indexer-verifier/templates/verifier-server/<chain>-verifier-config.json`
    "BTCApiKey1":"$(GENERATE_RANDOM_PASSWORD_16)",
    "DOGEApiKey1":"$(GENERATE_RANDOM_PASSWORD_16)",
    "XRPApiKey1":"$(GENERATE_RANDOM_PASSWORD_16)",

}