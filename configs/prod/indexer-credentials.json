{
    "indexerDatabase" : {
        // indexer requires indexer database with read-write access

        // server type. supported database types "mysql", "postgres", "sqlite" (default is "mysql")        
        //"type": "mysql",

        // server host address (default is "localhost")
        //"host": "127.0.0.1",

        // server port number (default is 3306)
        //"port": 3306,


        // database name
        "database": "indexer",

        // database username
        "username": "",

        // database password 
        "password": ""
    }
}
