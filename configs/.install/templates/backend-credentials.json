{
    "port": 9510,
    "web": {
        "rpcUrl": "$($(Network)BackendURL)"
    },
    "attesterDatabase": {
        "database": "$(AttesterDatabase$(Network))",
        "username": "$(AttesterReaderUsername$(Network))",
        "password": "$(AttesterReaderPassword$(Network))"
    }
}