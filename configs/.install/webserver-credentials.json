{    
    // Attestation client web serve port
    // For docker deployment this port should remain 3000.
    // External docker ports should be set in docker-compose yaml files during the deployment.
    "WebserverPort":3000,
}