{
   "verifierRoutes": [
      {
         "sourceId": "XRP",
         "routes": [
            {
               "attestationTypes": ["Payment", "BalanceDecreasingTransaction"],
               "url": "$(XRPVerifierRouterUrl)",
               "apiKey": "$(XRPVerifierRouterApiKey)"
            }
         ]
      },
      {
         "sourceId": "BTC",
         "routes": [
            {
               "attestationTypes": ["Payment", "BalanceDecreasingTransaction", "ConfirmedBlockHeightExists"],
               "url": "$(BTCVerifierRouterUrl)",
               "apiKey": "$(BTCVerifierRouterApiKey)"
            }
         ]
      }
  ]
}